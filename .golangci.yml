version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - gocritic
    - misspell
    - nilerr
    - unconvert
    - unparam

formatters:
  enable:
    - goimports

  settings:
    goimports:
      local-prefixes:
        - github.com/robince/parqview
